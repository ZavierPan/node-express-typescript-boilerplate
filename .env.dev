# Development Environment Configuration
# This file contains development-specific environment variables

# Application Configuration
PORT=3000

# JWT Configuration (Development - shorter expiry for testing)
JWT_SECRET=dev-jwt-secret-key-for-development-only-not-secure
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=24h

# CORS Configuration (Development - more permissive)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://localhost:5173
CORS_CREDENTIALS=true

# API Configuration (Development)
API_PREFIX=/api
SWAGGER_ENABLED=true

# Database Configuration (Docker - use service name)
DB_TYPE=mysql
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
DB_DATABASE=node_express_boilerplate
DB_SYNCHRONIZE=false
DB_LOGGING=true

# MySQL Configuration (for MySQL service)
MYSQL_ROOT_PASSWORD=password
MYSQL_DATABASE=node_express_boilerplate