# Production Environment Configuration
# ⚠️  IMPORTANT: This is a TEMPLATE file. Do NOT commit actual secrets to git!
# Copy this file and fill in with actual production values.
# Keep your actual .env.production file secure and never commit it.

# Application Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration (Production - CHANGE THESE!)
# Generate strong secrets: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_IN_PRODUCTION
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration (Production - restrict to your frontend domain)
CORS_ORIGIN=https://yourdomain.com,https://www.yourdomain.com
CORS_CREDENTIALS=true

# API Configuration (Production)
API_PREFIX=/api
SWAGGER_ENABLED=false

# Database Configuration (Docker - use service name)
DB_TYPE=mysql
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=CHANGE_THIS_TO_A_STRONG_DATABASE_PASSWORD
DB_DATABASE=node_express_boilerplate
DB_SYNCHRONIZE=false
DB_LOGGING=false

# Logging Configuration (Development)
LOG_LEVEL=info
LOG_ENABLE_FILE=true
LOG_MAX_FILE_SIZE=20m
LOG_MAX_FILES=14d
LOG_ENABLE_HTTP=true

# MySQL Configuration (for MySQL service)
# ⚠️  MUST match DB_PASSWORD above
MYSQL_ROOT_PASSWORD=CHANGE_THIS_TO_A_STRONG_DATABASE_PASSWORD
MYSQL_DATABASE=node_express_boilerplate

# Optional: Additional Production Settings
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000
# LOG_LEVEL=info
